# Alias Bot для Telegram 🎲
[![Docker](https://img.shields.io/badge/Docker-ready-blue.svg)](https://www.docker.com/)
[![Python](https://img.shields.io/badge/Python-3.10+-yellow.svg)](https://www.python.org/)

Профессиональный Telegram-бот для игры в "Alias". Проект оптимизирован для работы в Docker, обладает высокой производительностью и модульной архитектурой.

---

## 🚀 Основные фичи

*   **⚡ Высокая производительность**: Кэширование словарей в памяти и асинхронный ввод-вывод (`aiofiles`).
*   **🐳 Docker-ready**: Полная поддержка контейнеризации и Docker Compose.
*   **💾 Автоматическая персистентность**: Данные пользователей (`user_data.json`) и словари автоматически сохраняются в корне проекта на хост-машине.
*   **⚙️ Гибкая настройка**: Все параметры (токен, пути, админы, язык) вынесены в `.env`.
*   **🔒 Безопасность**: Ограничение админских функций (`/addword`, `/dict_upload`) по ID пользователя.
*   **🧩 Модульная архитектура**: Чистый и структурированный код, разделенный на логические блоки (Handlers, Config, Data).
*   **🌍 Мультиязычность**: Полная поддержка русского и английского языков (RU по умолчанию).

---

## 🛠 Быстрый старт (через Docker)

1.  **Клонируйте репозиторий**:
    ```bash
    git clone https://github.com/renkagod/Alias.git
    cd Alias
    ```

2.  **Настройте окружение**:
    Скопируйте пример конфига и впишите свой `BOT_TOKEN`:
    ```bash
    cp .env.example .env
    ```

3.  **Запустите проект**:
    ```bash
    docker compose up -d --build
    ```

---

## ⚙️ Переменные окружения (.env)

| Переменная | Описание | Значение для Docker (в docker-compose) |
| :--- | :--- | :--- |
| `BOT_TOKEN` | Токен вашего бота от @BotFather | **Обязательно** |
| `ADMIN_IDS` | ID администраторов (через запятую) | *Пусто (доступ всем)* |
| `DEFAULT_LANG` | Язык для новых пользователей | `ru` |
| `LOG_LEVEL` | Уровень логирования | `INFO` |
| `DICT_PATH` | Путь к папке со словарями | `/app/dictionaries/` |
| `USER_DATA_FILE` | Файл с данными пользователей | `/app/persistent/user_data.json` |

> **Примечание:** В Docker-версии пути `DICT_PATH` и `USER_DATA_FILE` уже преднастроены в `docker-compose.yml` и будут автоматически сохраняться в вашей локальной папке проекта.

---

## 📂 Структура проекта

```text
Alias/
├── app/                # Основной код бота
│   ├── handlers/       # Обработчики команд и кнопок
│   ├── config.py       # Настройки и логирование
│   ├── data_manager.py # Работа с файлами и кэшем
│   ├── texts.py        # Локализация
│   └── ui.py           # Генератор клавиатур
├── dictionaries/       # Сюда можно класть свои .txt словари
├── user_data.json      # Файл данных (создается автоматически)
├── Dockerfile          # Образ для Docker
├── docker-compose.yml  # Стек для развертывания
├── main.py             # Точка входа
└── requirements.txt    # Зависимости
```

---

## 🤖 Команды бота

*   `/start` — Регистрация и выбор словаря.
*   `/dict_upload` — Загрузка своего `.txt` (только для админов).
*   `/addword` — Добавить слово в существующий словарь (только для админов).
*   `/cancel` — Отмена текущего действия.

---

## 👨‍💻 Разработка

Автор: **renkagod** ([hakuworking@gmail.com](mailto:hakuworking@gmail.com))

Если вы хотите запустить бота без Docker:
1. Установите зависимости: `pip install -r requirements.txt`
2. Запустите: `python main.py`

---

## 📜 Лицензия
Проект распространяется "как есть". Пользуйтесь, дорабатывайте, играйте!
